# All dependencies resolve from the public npm registry.
# The @midnight-ntwrk/* packages (ledger-v8, testkit-js, midnight-js-*, wallet-sdk,
# compact-js, compact-runtime) are published there — no private registry or token
# is required. Do NOT add an auth token here.
registry=https://registry.npmjs.org/
